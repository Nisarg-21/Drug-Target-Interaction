SOLVER:
  MAX_EPOCH: 100
  BATCH_SIZE: 64
  NUM_WORKERS: 0
  LR: 0.00005
  DA_LR: 0.001
  SEED: 42
DECODER:
  BINARY: 1
COMET:
  PROJECT_NAME: CMA_AttnPooling
  TAG: Ablation_AttnPooling

# ablation-attn-pooling: learned weighted pooling replacing uniform mean.
# Identical to configs/CMA.yaml except for the flag below.
MODEL:
  POOLING: "attention"
