SOLVER:
  MAX_EPOCH: 100
  BATCH_SIZE: 64
  NUM_WORKERS: 0
  LR: 0.00005
  DA_LR: 0.001
  SEED: 42
DECODER:
  BINARY: 1
COMET:
  PROJECT_NAME: CMA_Residual
  TAG: Ablation_Residual

# ablation-residual: projected-GCN residual on fusion output.
# Identical to configs/CMA.yaml except for the flag below.
MODEL:
  USE_FUSION_RESIDUAL: True
